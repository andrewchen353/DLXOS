Lab5 was full of weird issues for us.

In newfs (our version of fdisk) we had to hard code the block numbers for the fbv blocks for initialization.

For DFS we had many issues with locks. We were able to create the lock correctly but were unable to acquire any of the locks afterwards. So, we just finished DFS without using any locks.

For the files api, we wrote all the code but had major issues with debugging since print statements were not working in files.c. To our knowledge q5 and q6 work. The print statments output ÿ repeatedly. But we should be ok with it.

We tried on the multilevel stuff...but finals are a thing. feelsbadman
